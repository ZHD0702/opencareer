# 工作日志 - 2026-05-12

## 已完成的任务

### 1. 响应模板与 server.py 分离
- 将 `_EMOTION_RESPONSES`、`_SUPPORT_TIPS` 迁移到 `mcp/prompts/emotion_support.yaml`
- 将 `_CAREER_TOPICS` 迁移到 `mcp/prompts/career_advice.yaml`
- 将 `_COMMON_QUESTIONS` 迁移到 `mcp/prompts/interview_prep.yaml`
- 创建 `mcp/prompts/__init__.py` 作为 YAML loader，支持缓存加载
- `server.py` 从硬编码改为通过 loader 加载模板，代码从 ~360 行精简到 ~260 行

### 2. Skill 自动转化机制
- 创建 `mcp/prompts/skills.yaml` 定义 Skill 格式（name、description、parameters、steps）
- 在 `mcp/prompts/__init__.py` 中实现 `SkillRegistry` 类：
  - 从 YAML 读取 Skill 定义
  - 自动为每个 Skill 创建 executor 函数，按步骤调用基础 Tool
  - 通过 `@mcp.tool` 将 Skill 注册为 MCP Tool
- 修改 `server.py`，在启动时自动注册所有 Skill
- 验证通过：注册了 4 个基础 Tool + 1 个自动生成的 `job_prep_full` Skill Tool

## 未完成的任务

- 为 Skill 定义增加参数校验（YAML 中的 parameters 未生成 JSON Schema 传给 MCP）
- 支持多步依赖（当前步骤是顺序执行，无法将上一步结果作为下一步输入）
- LangChain Agent 层的代码尚未编写（仅完成了 MCP Server 端的 Tool/Skill 暴露）

## 正在讨论的问题

1. **MCP Tool vs LangChain Tool 的区别** — 确认了 MCP Tool 是远程函数，LangChain Tool 是本地对象，通过 `MultiServerMCPClient` 桥接
2. **Skill 与 Tool 的关系** — Skill 是 Agent 层概念，MCP 协议中只有 Tool；通过 `@mcp.tool` 包装函数可以实现 Skill → Tool 的转换
3. **MCP 在项目中的定位** — MCP Server 充当 Tool Provider，将 Python 函数暴露给 AI 调用，起到工具服务层的作用
4. **下一步架构方向** — 用户计划用 LangChain Agent 框架做 Skill 编排，将 MCP Server 提供的 Tool 与自定义 Skill 结合使用
